/documents/overheadexpenseallocation:
  post:
    tags: [OverheadExpenseAllocation]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/documents/OverheadExpenseAllocation.yaml"
    responses:
      '201':
        description: Created
        content:
          application/json:
            schema:
              $ref: "../../components/schemas/documents/OverheadExpenseAllocation.yaml"
      '401':
        $ref: "../../components/responses/Unauthorized.yaml"
